defmodule Origami.ProcessorTest do
  use ExUnit.Case

  import Origami.Processor

  test "store assignment @key = \"value\"" do
    # ref = push(socket, "ping", %{"hello" => "there"})
    # assert_reply ref, :ok, %{"hello" => "there"}
  end
end
